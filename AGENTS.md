# プロジェクト方針

## 目的

このサイトを、不定期に小さな更新を重ねても、理解しやすく安全に変更できる状態に保つ。

## 変更の原則

- 依頼された変更を、既存の設計に沿った最小限の差分で実現する。
- その場しのぎの追加によって、重複や複雑さを増やさない。
- 将来の変更が難しくなる場合は、実装前に整理方法と影響を説明する。
- 見た目だけでなく、PC・スマートフォン・既存機能への影響を確認する。
- 判断に迷う場合は「将来の自分が安全に変更できるか」を優先する。

## 本番公開

ユーザーの「本番公開して」「公開して」「デプロイして」は、レビュー済みのPRを検証し、Cloudflare Pagesの本番環境へ公開することを意味する。

本番公開時は、必ず `docs/production-release.md` を最初から最後まで読み、その手順と停止条件に従う。

`main` への直接push、force push、PRを経由しない本番変更、検証を省略した公開は禁止する。権限・認証・CI・マージ条件に問題がある場合は、何も変更せず原因を報告して停止する。本番サイトで反映を確認するまでは確認用ブランチを保持し、確認後は削除してブランチ一覧を整理する。
